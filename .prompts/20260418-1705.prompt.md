Before copying the content to the output directory, make sure the output directory is empty. If it is not empty, please clear the contents of the output directory before proceeding.

However, do NOT delete:

- the .git directory
- the .gitignrore file (if it exists)