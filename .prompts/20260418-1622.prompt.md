In the current implementation, only files in the top level of the input directory are processed.

I want support for processing subdirectories as well.

Posts (`*.md`), `index.md`, and `rss.json` files will remain at the top level of the input directory.
But other assets (images, CSS, JS/TS) may be in subdirectories.